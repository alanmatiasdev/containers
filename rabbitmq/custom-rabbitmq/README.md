# Referências Bitnami

Este diretório mantém cópias exatas da árvore publicada pela Bitnami para fins de auditoria. Não edite estes arquivos; caso precise comparar com a imagem internalizada, use `containers/custom-rabbitmq`.

- `BITNAMI.md` contém o README original vindo do upstream.
- `4.1/`, `4.2/` etc. preservam Dockerfiles, scripts e `docker-compose` originais.
